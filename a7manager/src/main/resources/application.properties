spring.application.name=a7manager

# Dados de conexão
spring.datasource.url=jdbc:postgresql://localhost:5432/a7manager
spring.datasource.username=postgres
spring.datasource.password=550855

# Estratégia de criação de tabelas (update atualiza o banco conforme suas classes)
spring.jpa.hibernate.ddl-auto=update

# Mostra o SQL gerado no console (útil para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true